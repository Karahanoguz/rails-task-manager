<h1>Tasks</h1>
<hr>
<ul>
  <% @tasks.each do |task| %>
    <li> <%= link_to task.title, task_path(task.id) %> | <%= link_to "Edit Task", edit_task_path(task.id) %> | <%= link_to "Delete Task", task_path(task), method: :delete, data: { confirm: "Are you sure?" } %> </li>
  <% end %>
</ul>
<ul>
  <li> <%= link_to "Create Task", tasks_new_path %> </li>
</ul>
